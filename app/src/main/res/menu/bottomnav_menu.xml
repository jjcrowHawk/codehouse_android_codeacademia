<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/bottom_mensajes"
        android:title="Mensajes"
        android:icon="@drawable/messages"
        />

    <item android:id="@+id/bottom_anuncios"
        android:title="Anuncios"
        android:icon="@drawable/posts"
        />

    <item android:id="@+id/bottom_tareas"
        android:title="Tareas"
        android:icon="@drawable/tasks"
        android:iconTint="@android:color/white"
        />
</menu>